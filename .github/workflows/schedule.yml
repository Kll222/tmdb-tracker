name: TMDB Tracker Automation

on:
  schedule:
    - cron: "0 2,14 * * *"  # 每天 02:00 和 14:00 UTC
  workflow_dispatch:         # 允许手动触发

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 获取仓库内容
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # 3. 安装依赖
      - name: Install dependencies
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 4. 运行 TMDB 脚本生成 output.json
      - name: Run TMDB script
        run: python main.py
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}

      # 5. 检查输出文件
      - name: Check output
        run: |
          ls -R
          echo "Show first 500 chars of output.json:"
          head -c 500 output/output.json

      # 6. 上传整个仓库作为 GitHub Pages artifact
      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./    # 注意这里是整个仓库根目录，包括 index.html 和 output/

      # 7. 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
